database:
  override:
    - echo "Nothing to do here"

machine:
  ruby:
    version: 2.2.3

notify:
  webhooks:
    - url: https://xrayci.kap.la/webhooks/builds

test:
  override:
    - bundle exec rake
